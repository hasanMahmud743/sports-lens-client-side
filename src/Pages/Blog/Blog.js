import React from 'react';
import useTitle from '../../UseTitle/UseTitle';

const Blog = () => {
    useTitle('Blog')
    return (
        <div>
            
        </div>
    );
};

export default Blog;